# Binaries and test artifacts
/bin/
/dist/
*.exe
*.test
*.out
coverage.out
coverage.html

# Local toolchain caches
.gocache/
.gomodcache/

# go test -fuzz crashers written next to tests. Committed seeds live in
# testdata/corpus/{syslogwire,syslogframing} (GA-001).
**/testdata/fuzz/

# Minted bearer for compose smoke; never commit secrets
testdata/container/token

# Frontend (embedded copy lives in internal/web/dist)
web/node_modules/
web/dist/
web/coverage/

# Editor and OS noise
.idea/
.vscode/
*.swp
*.swo
.DS_Store
