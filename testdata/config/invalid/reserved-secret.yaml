apiVersion: labsyslog.dev/v1alpha1
kind: LabSyslog
metadata:
  name: lab-sink
spec:
  auth:
    mode: bearer
    tokens:
      - id: operator
        role: administrator
        secret: inline-secret-is-forbidden-and-long-enough
